[project]
name = "origami-api"
version = "0.1.0"
description = "Core configurations, data models, and stateless routing interfaces for EdgeRouter."
dependencies = [
    "pydantic>=2.9.2",
]
readme = "README.md"
requires-python = ">= 3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
