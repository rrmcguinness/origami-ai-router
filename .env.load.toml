[application]
name = "origami-router-load"

[load_test]
# Global fallback model if not overridden by specific test parameters
model = "gemini"
api_key = "[ENCRYPTION_KEY]"

# Load test scaling configurations
total_requests = 100
concurrent_clients = 4
server_port_gemini = 8006
server_port_vllm = 8005
server_port_llama = 8007
